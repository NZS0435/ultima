classDiagram
    class Scheduler {
        -TCB* process_table
        -int current_running_task
        +create_task(char* task_name, void* func_ptr) int
        +kill_task(int task_id) void
        +yield() void
        +garbage_collect() void
        +block_task(int task_id) void
        +unblock_task(int task_id) void
        +dump(int level) void
    }

    class TCB {
        +int task_id
        +string task_name
        +State task_state
        +void* context_pointer
    }

    class Semaphore {
        -char[64] resource_name
        -int sema_value
        -queue~int~* sema_queue
        +Semaphore(const char* name, int initial_value)
        +~Semaphore()
        +down() void
        +up() void
        +dump(int level) void
    }

    class State {
        <<enumeration>>
        RUNNING
        READY
        BLOCKED
        DEAD
    }

    Scheduler "1" *-- "*" TCB : manages
    TCB --> State : has
    Semaphore ..> Scheduler : interacts with
